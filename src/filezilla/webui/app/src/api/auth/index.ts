import fetch from './fetch';
import login from './login';
import logout from './logout';
import errors from './errors';
import refresh from './refresh';

import isLoggedIn from './isLoggedIn';
import isNeeded from './needed';

export default {
    fetch,
    login,
    logout,
    refresh,
    isLoggedIn,
    isNeeded,
    errors,
};
